'use strict';

describe('myApp.plane module', function () {

  beforeEach(module('myApp.plane'));

  describe('plane controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var PlaneCtrl = $controller('PlaneCtrl');
      expect(PlaneCtrl).toBeDefined();
    }));

  });
});