'use strict';

describe('myApp.sliders module', function() {

  beforeEach(module('myApp.sliders'));

  describe('sliders controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var SlidersCtrl = $controller('SlidersCtrl');
      expect(SlidersCtrl).toBeDefined();
    }));

  });
});