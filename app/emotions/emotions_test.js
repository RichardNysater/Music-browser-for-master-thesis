'use strict';

describe('myApp.emotions module', function () {

  beforeEach(module('myApp.emotions'));

  describe('emotion controller', function () {

    it('should ....', inject(function ($controller) {
      //spec body
      var PlaneCtrl = $controller('EmotionsCtrl');
      expect(PlaneCtrl).toBeDefined();
    }));

  });
});